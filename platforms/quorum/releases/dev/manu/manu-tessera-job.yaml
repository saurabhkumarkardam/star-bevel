apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: manu-tessera-job
  namespace: manu-quo
  annotations:
    flux.weave.works/automated: "false"
spec:
  releaseName: manu-tessera-job
  interval: 1m
  chart:
   spec:
    chart: platforms/quorum/charts/quorum-tessera-key-mgmt
    sourceRef:
      kind: GitRepository
      name: flux-quo-1
      namespace: flux-quo-1
  values:
    peer:
      name: manu
    metadata:
      name: manu-tessera-job
      namespace: manu-quo
    image:
      repository: quorumengineering/tessera:hashicorp-21.7.3
      pullSecret: regcred
    vault:
      address: http://dev-vault-public.dev.aws.blockchaincloudpoc-develop.com
      secretengine: secretsv2
      authpath: quo-1manu
      keyprefix: manu/crypto
      role: vault-role
      serviceaccountname: vault-auth
      tmprefix: secretsv2/data/manu/crypto
      type: hashicorp
